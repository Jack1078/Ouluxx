import React from 'react';
import PropTypes from 'prop-types';
import classes from './navBar_k.module.css';

const NavBar = (props) => {
    return (
        <div className={classes.nav_bar}>abc</div>
    );
}

export default NavBar;